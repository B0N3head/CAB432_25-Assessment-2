# Client environment configuration for split architecture

# API Base URL (variable name must match App.jsx: VITE_API_BASE)
# When deployed: Use load balancer URL since both frontend and backend are behind it
VITE_API_BASE=http://group46-video-editor-1562782604.ap-southeast-2.elb.amazonaws.com

# For HTTPS (if configured)
# VITE_API_BASE=https://videoeditor.cab432.com

# For local development
# VITE_API_BASE=http://localhost:3000
