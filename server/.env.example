JWT_SECRET=devsecret
PORT=3000
AWS_REGION=ap-southeast-2

# Cognito (set to enable Cognito auth + groups + MFA)
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_DOMAIN=

# S3 (set to enable presigned uploads and downloads)
S3_BUCKET=
S3_UPLOADS_PREFIX=uploads/
S3_OUTPUTS_PREFIX=outputs/
S3_THUMBS_PREFIX=thumbnails/

# ElastiCache Memcached (optional caching and SSE progress store)
MEMCACHED_URL=
